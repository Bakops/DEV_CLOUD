version: '3.8'

services:
  app:
    build:
      context: ./cloud
      dockerfile: Dockerfile
    image: cloud-app:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
